# Second project for Udacity React Nanodegree

* Install and start the api-server
    - `cd api-server`
    - `npm install` or `yarn install`
    - `node server`
* In another terminal window
    - `create-react-app frontend`
    - `cd frontend`
    - `npm start`
